
@using Cfm.Web.Mvc.Common;
@{
    //int total = int.Parse(ViewBag.Total.ToString());
    //int pageIndex = int.Parse(ViewBag.PageIndex.ToString());
    //int pageSize = int.Parse(ViewBag.PageSize.ToString());
    string reportType = ViewBag.reportType;
    string ItemType = ViewBag.ItemType;
    int TransferType = (int)ViewBag.TransferType;
    //int stt = (pageIndex - 1) * pageSize;
    string refType = ((int)Constant.RefType.ItemList).ToString().PadLeft(3, '0');
}
<div class="panel panel-primary">
    <div class="panel-heading">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4>
            Chọn chỉ tiêu
        </h4>
    </div>
    <div class="panel-body">
        <div class="filter-bar">
            <div class="form-group">
                <label>Thông tin tìm kiếm</label>
                <div class="input-group">
                    <input class="form-control text-uppercase" id="txtSearchItem" placeholder="Mã chỉ tiêu, tên chỉ tiêu" type="text">
                    <span class="input-group-btn">
                        <button id="search_item" onclick="Transfer_Process.SearchItem(@refType,1)" name="search_item" class="btn btn-pns btn-cons" type="button"><i class="fa fa-fw fa-search"></i> Tìm kiếm</button>
                    </span>
                </div>

            </div>
        </div>
        <div id="ListDicItem">

            @*<div class="table-responsive maxheight450">
                <table id="tblListItem" class="table table-striped table-bordered datatables dataTable no-footer">
                    <thead>
                        <tr class="tr_head">
                            <td class="w40 text-center"></td>
                            <td class="w80 text-center">ID</td>
                            <td class="w180 text-center">Mã Chỉ tiêu</td>
                            <td class="text-left">Tên Chỉ tiêu</td>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Count > 0)
                        {
                            for (int i = 0; i < Model.Count; i++)
                            {
                                //var item = Model.ListItem[i];

                                <tr>
                                    @Html.HiddenFor(x => Model[i].Id)
                                    <td class="text-center">
                                        @Html.CheckBoxFor(x => Model[i].Selected)
                                    </td>
                                    <td class="text-left">
                                        @Model[i].Id
                                    </td>
                                    <td class="text-left">
                                        @Model[i].Code
                                    </td>
                                    <td class="text-left">
                                        <span>@Model[i].Name</span>
                                    </td>
                                </tr>
                            }
                            <tr>
                                <td colspan="4">
                                    @Html.Raw(Utility.Page(total, pageIndex, 3, pageSize, (((pageIndex <= 0 ? 1 : pageIndex) - 1) * pageSize) + 1, pageSize * (pageIndex <= 0 ? 1 : pageIndex), "Transer_Process.SearchItem", refType, false))
                                </td>
                            </tr>
                        }
                        else
                        {
                            <tr>
                                <td colspan="3">
                                    <span>
                                        Không tìm thấy dữ liệu...!
                                    </span>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>*@
            @{ Html.RenderAction("ItemListGet", "Mapping", new { @Area = "Admin" });}
        </div>
        <div class="modal-footer">
            <div class="row">
                <button type="button" onclick="Transfer_Process.GetSelectedItem('@ItemType')" class="btn btn-pns btn-sm"><i class="fa fa-check"></i> Đồng ý</button>
                <button type="button" class="btn btn-danger btn-sm" data-dismiss="modal"><i class="fa fa-times" aria-hidden="true"></i> Thoát</button>
            </div>
        </div>
    </div>
</div>
 
<script>
    Transfer_Process.reportType = '@reportType';
    Transfer_Process.itemType = '@ItemType';
    Transfer_Process.TransferType = '@TransferType'
</script>



